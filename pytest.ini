[pytest]
testpaths = tests
python_files = test_*.py 
addopts = 
    --strict-config
    --cov=src/scoreai
    --cov-report=term-missing
    --cov-report=html:htmlcov
    --cov-report=xml:coverage.xml
    --cov-fail-under=20
asyncio_mode = auto
