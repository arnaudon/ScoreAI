services:
  db:
    ports:
      - "5432:5432"
  backend:
    ports:
      - '8000:8000'
    volumes:
      - ./backend:/app/backend
      - ./shared:/app/shared
      - ./database:/app/database
    environment:
      - DATA_PATH=/app/data

  frontend:
    ports:
      - '8501:8501'
    volumes:
      - ./frontend:/app/frontend
      - ./shared:/app/shared
      - ./data:/app/data
    environment:
      - API_URL=http://backend:8000

volumes:
  postgres_data:
