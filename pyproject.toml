[project]
name = "ScoreAI"
version = "0.1.0"
description = "scoreai monorepo"
dependencies = []

[tool.uv.workspace]
members = ["backend", "frontend", "shared"]

[tool.mypy]
mypy_path = "shared:backend:frontend"
explicit_package_bases = true

[tool.pytest.ini_options]
testpaths = ["backend/tests", "frontend/tests", "shared/tests"]
asyncio_mode = "strict"
